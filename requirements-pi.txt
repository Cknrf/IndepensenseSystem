# Raspberry Pi-only runtime dependencies. Install on the Pi with:
#   pip install -r requirements.txt -r requirements-pi.txt
pyserial>=3.5

# picamera2 is NOT installable via pip — it depends on system libcamera bindings.
# Install via apt:  sudo apt install -y python3-picamera2
# Then create the venv with --system-site-packages so it can be imported.

# YOLOv8 object detection.
#
# Torch is pinned because torch>=2.7 ships its wheel with hard dependencies on
# NVIDIA CUDA libraries (nvidia-cublas, nvidia-cudnn, ...) even on aarch64 Linux.
# On a Pi 5 (no NVIDIA GPU) those libraries are ~3 GB of dead weight.
# torch 2.5.1 is the latest version known to install cleanly on Pi 5 (aarch64,
# Python 3.13) without pulling CUDA libraries.
torch==2.5.1
torchvision==0.20.1
ultralytics>=8.0
